# ABORZAN — Python Library Installer

This project contains a Python script that automatically installs many popular Python libraries.  
It helps beginners and developers prepare their environment faster without typing installation commands manually.

The script also shows an animated logo (line-by-line), and — when running inside Termux — it opens the developer’s Facebook page.

---

## ✨ Features

- Installs dozens of common Python libraries automatically  
- Works on Termux and desktop environments  
- Animated logo with smooth text effect  
- Simple and beginner-friendly

---

## 🚀 How to Run

1. Install Python
2. Download or clone this repository
3. Open terminal inside the project folder
4. Run:

```bash
python Library-python.py
